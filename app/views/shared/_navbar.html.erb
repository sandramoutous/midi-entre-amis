<nav class="navbar navbar-light bg-light justify-content-between">
  <%= link_to 'Midi entre amis', root_path, class: 'navbar-brand mx-4' %>
  <div style="margin-right: 2em;">
      <% if user_signed_in? %>
        <div class="btn-group">
          <button type="button" class="btn btn-light dropdown-toggle" data-bs-toggle="dropdown" data-bs-display="static" aria-expanded="false">
            <%= current_user.email %>
          </button>
          <ul class="dropdown-menu dropdown-menu-end">
            <li><%= link_to "Mes infos", user_path(current_user), class: "dropdown-item" %></li>
            <li><button class="dropdown-item" type="button">Ma wishlist</button></li>
            <li><hr class="dropdown-divider"></li>
            <li><%= link_to "Se déconnecter", destroy_user_session_path, method: :delete, class: "dropdown-item" %></li>
          </ul>
        </div>
      <% else %>
      <ul class="navbar-nav">
        <li class="nav-item">
          <%= link_to "Se connecter", new_user_session_path, class: "nav-link" %>
        </li>
      </ul>
      <% end %>
  </div>
</nav>
